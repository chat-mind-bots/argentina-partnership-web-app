name: Dev Deployment

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest

    steps:
      # Checkout the code from the 'dev' branch
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: dev


      # Set up SSH key for authentication
#      - name: Set up SSH key
#        run: echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa

      # Install SSH client
      - name: Install SSH client
        run: sudo apt-get install -y openssh-client

      # Connect to the server and run the updateDev.sh script
      - name: Deploy to Server
        run: |
          sshpass -p "bYNT149k2Zid" ssh -o StrictHostKeyChecking=no root@5.159.102.190 "cd ~/argentina-partnership-deploy && bash updateDev.sh"
